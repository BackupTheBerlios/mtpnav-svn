FIXME:
* connect on demand instead of connect once
* Fix deleting more files (update model directly without reading device again, prevent multy model_update request when adding many small files, mutex on connect)
* display used space in Kb/Mb/Gb

FEATURES 0.1:
* trace debug and exceptions
* size/resize sort columns
* progress bar for transfer window
* add button to cancel / retry on error job in transfer window
* add metadata from mp3/ogg tags. Get metadata from directory/file scheme if no tag
* file view
* add and manage playlist
* rigth click context menu
* preferences

FEATURES post 0.1:
* edit metadata
* copy and paste from nautilus
* album view
* multi device
* support for fs based device
* cover

PRERELEASE:
* logo
* about dialog
* translation
* setup
* deb
* web site
