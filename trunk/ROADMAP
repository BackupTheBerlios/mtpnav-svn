
RELEASE 0.1.0:
--------------
    FIXME:
    *       del key for delteting file
    * TEST: cancelling current job block other jobs
    *       seg fault when deleting many files
    *       prevent deleting system folders and files (waiting response from libmtp list)
    * TEST: select the row under mouse when drag and dropping
    *       fix metadata and filenames encoding (accent and special charaters)
    *       refresh free space progress bar on each operation
    *       check if job already present before adding
    *       refuse adding others than tracks in track view ? 
    *       identify from device what is a track and what is a file

    PRERELEASE 0.1.a1: RELEASED
    * DONE: view device information
    * DONE: view track listing
    * DONE: add file to device
    * DONE: add file to device per drag and drop
    * DONE: delete files
    * DONE: job queue

    PRERELEASE 0.1.a2: IN DEVELOPPMENT
    * TEST: confirm before deleting
    * DONE: file view
    *       create directory in file view
    * DONE: remove a whole directory in file view
    * DONE: add file to directory in file view
    *       move files/directories
    * DONE: button to cancel in transfer window 
    *       identify transfers errors (already on device/device full)
    *       connect on demand instead of connect once
    *       add metadata from ogg tags
    *       Columns resisable
    * TEST: date into metadata
    *       trace debug and exceptions

    PRERELEASE 0.1.a3: PLANNED
    * add a whole directory (drag and drop + menu item). Only track added in track view
    * add and remove playlist
    * add tracks to playlist
    * get file from device
    * preferences
    * parametrize metadata from directory/file scheme if no tag
    * rigth click context menus
    * display tooltips on track view and job list (artist, title genre...)
    * button to cancel job /retry on error at row level

    RELEASE 0.1.0beta0 PLANNED
    * test and fixe

    RELEASE 0.1.0 PLANNED
    * no know bugs
    
    RELEASE 0.1.x PLANNED
    * bug fixes

    PRERELEASE:
    * TEST: setup
    *       central version number + svn revision + changelog
    *       logo
    *       about dialog
    *       translation
    *       deb
    *       web site
    *       gnome menu entry

RELEASE >= 0.2.0:
--------------
* accept samba files
* edit metadata
* copy and paste from nautilus
* album view
* multi device
* support for fs based device
* cover
* backup/restore device
* choose columns to display
* play tracks
